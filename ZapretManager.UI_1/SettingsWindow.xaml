<Window x:Class="ZapretManager.UI_1.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZapretManager.UI_1"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        WindowStyle="None"
        mc:Ignorable="d"
        Background="Transparent"
        Title="Settings"
        
        MinHeight="480" MinWidth="800"
        MaxHeight="500" MaxWidth="800"
        
        Height="480" Width="800"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        >

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding RunDataBaseCommand}" />
            <i:InvokeCommandAction Command="{Binding LoadEnvironmentCommand}" />
        </i:EventTrigger>

        <i:EventTrigger EventName="Closed">
            <i:InvokeCommandAction Command="{Binding CloseEnvironmentCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/ZapretManager.UI_1;component/Assets/Main.png" Stretch="UniformToFill"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <TextBlock Style="{StaticResource HeaderTextBlock}" Grid.Column="0" Text="Settings" HorizontalAlignment="Left" 
                VerticalAlignment="Center" Margin="2.5,0,0,0" Padding="10"/>
        </Grid>

        <Grid Grid.Row="1">
            <Border Height="2" Background="#FF6B5B" CornerRadius="1" Margin="10,0,10,10" 
                VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,5,0,15">
            <Border Height="2" Background="Transparent" CornerRadius="1" Margin="10,0,10,10" 
                VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height ="Auto" />
                <RowDefinition Height ="Auto" />
                <RowDefinition Height ="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width = "Auto"/>
                <ColumnDefinition Width = "Auto"/>
                <ColumnDefinition Width = "Auto"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid Grid.Row="0" Margin="2.5,0,0,0">
                    <ItemsControl Grid.Column="0" Margin="50,0,0,0" ItemsSource="{Binding LeftSettings}"/>
                </Grid>
            </Grid>

            <Grid Grid.Column="1">
                <Grid Grid.Row="1" Margin="2.5,0,0,0">
                    <ItemsControl Grid.Column="1" Margin="50,0,0,0" ItemsSource="{Binding MiddleSettings}"/>
                </Grid>
            </Grid>

            <Grid Grid.Column="2">
                <Grid Grid.Row="2" Margin="2.5,0,0,0">
                    <ItemsControl Grid.Column="2" Margin="50,0,0,0" ItemsSource="{Binding RightSettings}"/>

                    <Button Style="{StaticResource MainButton}" Padding="10" Margin="50,0,0,0" Command="{Binding ApplySettingsCommand}"
                        MinWidth="150" MinHeight="60" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                        <TextBlock Style="{StaticResource HeaderTextBlock}" FontSize="18"  Text="Apply" HorizontalAlignment="Right" 
                                VerticalAlignment="Center" Padding="7"/>
                    </Button>

                </Grid>
            </Grid>

        </Grid>

    </Grid>

</Window>